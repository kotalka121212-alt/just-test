add_library(main_lib INTERFACE)
add_library(inc_lib INTERFACE)


add_subdirectory(grafics)
add_subdirectory(source_libs)
add_subdirectory(src/modules)
add_subdirectory(src/nn)






target_include_directories(main_lib INTERFACE 
${CMAKE_CURRENT_SOURCE_DIR}/src/nn
${CMAKE_CURRENT_SOURCE_DIR}/src

)

target_link_libraries(inc_lib INTERFACE
all_libs nexus_modules nexus_core_libs 
)


target_link_libraries(main_lib INTERFACE
                                     
            

       


)